import React from 'react';

const Header:React.FC = () => (
  <header className="header">
    <div className="header__logo">V.H</div>
  </header>
);
// eslint-disable-next-line semi

export default Header;
